namespace DeepConfigEditor.Utilities
{
    using System;

    public static class As
    {
        public static Uri ResourceUri(string iconResource)
        {
            return ResourceHelper.ResourceUri(iconResource);
        }
    }
}
